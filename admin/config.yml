backend:
  name: git-gateway
  branch: main
  commit_messages:
    create: "Create {{collection}} '{{name}}' via CMS"
    update: "Update {{collection}} '{{name}}' via CMS"
  # GitHub OAuth
  base_url: https://api.github.com
  repo: predaalexiaa-ship-it/stormmachine.github.io  # Your repo
  site_id: Ov23liioIPaP5BOd2CN0  # From Step 1
